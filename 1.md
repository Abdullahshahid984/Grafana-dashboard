```
api-transactions service is failing to start with HTTP 500.

**What's happening:**
1. Pod starts and pulls the image successfully
2. App tries to initialize database connection to acsi6x014.database.windows.net
3. DNS resolution fails the cluster can't resolve external hostnames
4. Connection fails with "Connection reset"
5. Startup probe returns HTTP 500
6. Kubernetes kills the pod and restarts it (repeat)

**Evidence from logs:**
- App logs: "Connection reset... host acsi6x014.database.windows.net port 1433"
- App logs: "Could not obtain connection to query metadata"

**This affects all your database-connected services:**
- api-transactions (failing)
- api-statements (failing)
- api-pricing (failing)

```

Added excluded_cluster_names logic to the app_alerts module to prevent Prometheus alert rule group creation for restore/validation clusters. This fixes a recurring pipeline failure caused by Azure Backup restoring rule groups into the same resource group with identical names that Terraform also tries to manage.

kubectl get secret istio-ingress-gateway-cert-manager-tls -n aks-istio-ingress -o jsonpath="{.data['tls\.crt']}" | % { [System.Text.Encoding]::UTF8.GetString([System.Convert]::FromBase64String($_)) } | Out-File "$env:TEMP\tls.crt"

openssl x509 -in "$env:TEMP\tls.crt" -text -noout

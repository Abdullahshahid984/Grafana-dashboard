Still crashing. The restart count keeps climbing (1010, 237, 1291). That means it's not the ConfigMap/Secret issue alone - something deeper is wrong, probably RBAC.

I need you to run these commands to check the service account:

```powershell
# Get the service account name for this deployment
kubectl get deployment api-transactions -n api-transactions-v1 -o jsonpath='{.spec.template.spec.serviceAccountName}'
```

Once you have that, run:

```powershell
# Replace <sa-name> with the service account name from above
kubectl describe sa <sa-name> -n api-transactions-v1

# Check if it can read configmap
kubectl auth can-i get configmap --as=system:serviceaccount:api-transactions-v1:<sa-name> -n api-transactions-v1

# Check if it can read secrets
kubectl auth can-i get secrets --as=system:serviceaccount:api-transactions-v1:<sa-name> -n api-transactions-v1
```

And also check the actual pod logs to see if there's a permission error:

```powershell
kubectl logs api-transactions-854cbdb684-gzb7n -n api-transactions-v1 --tail=50
```

This will show the actual crash reason. My guess is it's hitting a permission denied error when trying to read the ConfigMap or Secret.

What's the output of those commands?

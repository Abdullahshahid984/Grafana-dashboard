if ($decompressed -match "profilebatchservice-api-config[\s\S]{0,500}secretProviders") {
    Write-Host "SMOKING GUN: secretProviders IS nested in ConfigMap - STALE CODE" -ForegroundColor Red
} else {
    Write-Host "secretProviders is NOT nested in ConfigMap" -ForegroundColor Green
}

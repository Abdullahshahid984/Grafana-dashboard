Perfect! Now use the correct NSG name:

```powershell
$nodeRG = "rg-bfhak-ihub-eus2-dev-01-nodes"
$nsgName = "eastus2-aks-agentpool-1285725-nsg"

# Check for DNS (port 53) outbound rules
az network nsg rule list -g $nodeRG --nsg-name $nsgName --query "[?direction=='Outbound' && (destinationPortRange=='53' || destinationPortRange=='*')]" -o table
```

Also check all outbound rules to see what's allowed:

```powershell
az network nsg rule list -g $nodeRG --nsg-name $nsgName --query "[?direction=='Outbound']" -o table
```

Run both and show me the output. We're looking for whether port 53 is explicitly allowed outbound to any destination.

If there are no rules allowing port 53, that's blocking your DNS queries and why external domains can't be resolved.

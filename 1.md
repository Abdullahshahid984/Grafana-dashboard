Hi Team,
During our AKS backup restore validation on the restore cluster aks-bfhaks-ihub-eus2-dev-rst-01, I has identified an issue affecting workloads that use Workload Identity.

**Issue**

Pods are failing to start due to Failed Mount errors on Key Vault secret mounts. The root cause is that the restore cluster has a different OIDC issuer URL than the original cluster. Because of this, Entra ID is rejecting the Workload Identity token exchange with the following error:
```
**AADSTS700211:** No matching federated identity record found for presented assertion issuer
This is happening because the Managed Identity federated credentials were only registered against the original cluster's OIDC issuer not the restore clusters.
```
**Restore Cluster OIDC Issuer URL:**

```
https://eastus2.oic.prod-aks.azure.com/7a24eae8-33b9-449a-83f5-361634c821ce/76909aed-b958-464f-8a83-02455e1cce47/
```

Please action this at your earliest convenience so I can proceed with restore validation. Let me know if you have any questions.

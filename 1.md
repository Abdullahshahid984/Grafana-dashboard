App Insights cloud_RoleName shows the call to kanasmc.alldata.net as originating from App Service appsvc-ads-cus-ebo-ei-cims-svc-prd-001 not AKS.

AKS pod (api-cims-v1), its Istio VirtualService, and the checked APIM instance have all been ruled out via direct evidence: clean pod logs (no reference to kanasmc.alldata.net), VirtualService has a single route pointing only to api-cims-service internally, and no APIM backend/API config references kanasmc.alldata.net.

# See ALL secrets in the namespace
kubectl get secret -n api-transactions-v1

# See ALL configmaps
kubectl get configmap -n api-transactions-v1

# Check the deployment to see what secret it's looking for
kubectl get deployment -n api-transactions-v1 -o yaml | Select-String -Context 0,30 "valueFrom"

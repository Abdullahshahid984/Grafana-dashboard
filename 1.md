# Check the keys in your ConfigMap
kubectl get configmap api-transactions-config -n api-transactions-v1 -o yaml

# Check the keys being synced from Azure Key Vault
kubectl get secret kvadseus2nonprdsitihub01-secrets-syncd -n api-transactions-v1 -o yaml

Added RST cluster (aks-bfhaks-ihub-eus2-dev-rst-01) to DEV conf.yaml to support AKS backup and restore validation configure new cluster, run pipeline to create it, run workload onboarding to create federated credentials , then restore backup.

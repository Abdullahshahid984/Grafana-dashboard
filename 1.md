CONTEXT
This is the APIM.Partner repo (Azure API Management config deployed by pipeline). I'm on a feature branch with an open Azure DevOps PR to main. Earlier I mistakenly added an operation to the Fiserv Rewards PLP API (APIM/apis/fiservrewardsplpapi). A reviewer pointed out that the ticket says the Fiserv Rewards Clutch API. I have already reverted the PLP files back to origin/main, so do not touch anything under fiservrewardsplpapi.

TICKET
"Add URL to Partner portal POST /auth/batch to DEV/SIT/UAT. Need https://cat-pl-plpcertificatemgmt.fdcsoa.com/auth/batch added to apim partner in DEV/SIT/UAT. Similar to the redemption batch already in Fiserv Rewards Clutch, just need to add auth/batch."

WHAT I LEARNED FROM THE PLP API (for reference only)
- The API's OpenAPI spec is in FiservRewardsPLP.yaml, and the same spec is embedded as one long "value" string (with literal \r\n escapes) in arm/FiservRewardsPLP.api.template.json.
- Operations are plain "POST /somePath" entries; the backend URL is NOT in the repo: the ARM template uses "serviceUrl": "[parameters('serviceUrl')]" and the parameters file only holds ApimServiceName, so the pipeline supplies serviceUrl per environment.
- An API-level policy (policies/auth-fiserv-two-way-tls.xml) applies mutual TLS to all operations.
- The Config.yml files are inputs to the APIM DevOps Resource Kit "creator", which generates the arm/ template.

MY PLAN
Add POST /auth/batch to the Clutch API by mirroring exactly how the redemption batch is defined there (yaml path + matching entry in the ARM template), assuming the backend host resolves to https://cat-pl-plpcertificatemgmt.fdcsoa.com via the API's serviceUrl.

YOUR TASK, in this order
Phase 1: Read-only investigation. Do NOT edit any file yet. Inspect APIM/apis/fiservrewardsclutchapi and report:
1. Files in the folder and its arm subfolder.
2. Exactly how the redemption batch is defined in the Clutch yaml (quote the block: path, operationId, request/response).
3. Whether the Clutch ARM template embeds the spec as one long string (like PLP) or defines operations as separate resources, and where the redemption batch appears.
4. Whether the redemption batch has an operation-level policy or backend override, or uses the API-level serviceUrl.
5. Which backend host the Clutch API uses, and whether it is the same as https://cat-pl-plpcertificatemgmt.fdcsoa.com.
6. Any per-environment files (DEV/SIT/UAT) in the repo that would also need the same change.

Phase 2: Assessment. Tell me plainly whether my plan is correct, and why or why not. Specifically flag:
- Whether the ticket really belongs in Clutch, or whether anything in the repo suggests otherwise.
- Whether the backend host is likely already right, or whether auth/batch needs an operation-level set-backend-service policy.
- Whether SIT/UAT are covered by the same files or need separate changes.
- Anything you cannot verify from the repo (for example the pipeline's serviceUrl values per environment), listed explicitly so I can check it in Azure DevOps.

Phase 3: Proposal. Give the exact minimal edits (file, location, text). Mirror the redemption batch pattern. Then STOP and wait for my approval.

Phase 4: Only after I approve, make the edits. Rules: change nothing else, do not reformat any file, keep the JSON valid, keep \r\n sequences as literal characters in the embedded string, do not commit or push. Afterwards show git diff --stat and a word-level diff (git --no-pager diff --word-diff=porcelain --word-diff-regex=. -- <file> | Select-String '^[+-]') proving that only additions were made.

Hi, The pipeline failure you are seeing is a code version issue, not a pipeline issue. Yesterday we removed the Terraform code for the RST cluster federated credentials and Prometheus alert rules from the workload onboarding pipeline, and successfully deployed through our pipeline with the latest changes. The error you are seeing indicates your branch is still pointing to an older version of the code that still has the RST cluster references. Please pull the latest changes from main into your branch and re-trigger the pipeline  that should resolve the issue. Let me know if you need any help.

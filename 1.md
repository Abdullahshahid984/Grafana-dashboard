Hey the Helm chart does support volume mounting via CSI Secret Provider it's already doing it for other secrets. However, no certificate is mounted yet for ProfileAPI. To add one, the cert needs to be stored in Key Vault first, then you will add it to the secretProviders block in the values with the desired mount path. 

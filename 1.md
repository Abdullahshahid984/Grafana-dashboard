I have completed the AKS backup and restore validation on POC-02. I restored 5 to 6 namespaces from the DEV cluster and pods came up successfully with Key Vault secrets mounting correctly.

The main technical challenge was Workload Identity each cluster has a unique OIDC issuer URL so after restore, pods get stuck in Init state because the Managed Identity federated credentials don't cover the restore cluster. I resolved this manually by adding federated credentials via the portal for each namespace.

However with 71 namespaces this manual approach is not scalable. I am currently researching automation options — either a PowerShell script or Terraform approach to automatically create federated credentials for all namespaces in one run.

**Two things I want your guidance on:**

1. Should I proceed with deploying backup in other environments (SIT, UAT, PERF) now, or wait until the federated credential automation is complete first?

2. Which automation approach should I prioritize PowerShell script  or Terraform?

Yes the secret is there in KV, that's confirmed. The issue is your values.yaml change hasn't been deployed to the cluster yet — the SecretProviderClass on the cluster still doesn't have CATOOLS-OKTA-SAML-CERTIFICATE in it. 

```

**Ticket 1: Grafana v12 Provider Support**

**Title:** Upgrade azurerm provider to v5.0+ for Grafana v12 support

**Description:**
Update Terraform provider version from azurerm 4.47.0 to 5.0+ to support Grafana major version 12. Required for June 15, 2026 deprecation deadline when Grafana v11 reaches end-of-life. Update in bfhaksplatform/terraform/main.tf and workload-onboarding/phases terraform.tf files.

---

**Ticket 2: Federated Identity Credentials - Lifecycle Management**

**Title:** Add lifecycle blocks to Federated Identity Credentials to prevent 409 conflicts

**Description:**
Fixed 409 Conflict errors when Phase 2 pipeline runs for restore clusters. Added `lifecycle { ignore_changes = all }` to both `azurerm_federated_identity_credential.workload_identity` and `azurerm_federated_identity_credential.workload_identity_additional` resources in workload-onboarding/modules/kubernetes-resources/main.tf. Enables idempotent deployments and multi-cluster workload identity federation.

---

**Ticket 3: Phase 2 Istio Gateway Configuration**

**Title:** Update Phase 2 locals to support Istio gateway for restore clusters

**Description:**
Modified workload-onboarding/phases/02-per-instance-per-cluster/locals.tf to properly configure Istio ingress gateway for both primary and restore clusters. Ensures correct subdomain resolution for both dev-01 and rst-01 clusters. Removes cluster-specific exclusions that prevented restore cluster gateway deployment.

---

**Ticket 4: Restore Cluster Infrastructure - Cleanup**

**Title:** Remove rst-01 restore cluster configuration from main branch

**Description:**
Removed all rst-01 cluster definitions from main branch including AKS cluster resource, node pools, and associated configurations. Ensures main branch contains only dev-01 cluster configuration. Cleaned up via Terraform destroy - cluster and all associated Azure resources successfully deleted.

---

**Ticket 5: Workload Onboarding Phase 2 - Revert Changes**

**Title:** Run Phase 2 pipeline to revert workload configurations after cluster deletion

**Description:**
Execute workload onboarding Phase 2 pipeline to clean up remaining Kubernetes resources and permissions for deleted rst-01 cluster. Removes Federated Identity Credentials, cleans up namespace references, and reverts dev-01 to original state. Final cleanup step after platform pipeline completion.

---

**Ticket 6: Feature Branch - Cleanup**

**Title:** Delete feat/ branch after restore cluster removal complete

**Description:**
Delete feature branch containing experimental rst-01 workload onboarding changes once Phase 2 revert pipeline completes. Ensures main branch remains clean with only dev-01 configuration. All changes have been reverted and are no longer needed.

---

**Ticket 7: Documentation - Restore Cluster Workload Onboarding**

**Title:** Document Federated Identity Credentials multi-cluster support

**Description:**
Document the lifecycle block pattern used to support multi-cluster workload identity federation. Each Managed Identity now trusts OIDC tokens from multiple cluster OIDC issuers, enabling workload restoration across clusters. Include reference to workload-onboarding/modules/kubernetes-resources/main.tf implementation.

---

**Ticket 8: AKS Platform - Restore Cluster Validation**

**Title:** Validate restore cluster deployment pattern and disaster recovery readiness

**Description:**
Document the successful deployment pattern for restore clusters including: Federated Identity Credentials for multi-cluster federation, Istio gateway configuration, Cert-Manager, Wiz connector, and CrowdStrike deployment. Confirm idempotent Phase 2 pipeline execution and rollback procedures.

---



# AKS-Side Checks Detailed Log

**Namespace:** `api-cims-v1`
**Pod:** `api-cims-56fbf694d6-5gfq9` (2/2 Running, 0 restarts, age ~106m+)
**Containers:** `api-cims` (app), `istio-proxy` (sidecar)

---

## 1. Pod Health
```
kubectl get pods -n api-cims-v1
```
**Result:** `api-cims-56fbf694d6-5gfq9   2/2   Running   0   106m`
Pod healthy, no restarts, no crash loops.

---

## 2. Deployment Check
```
kubectl get deployments -n api-cims-v1
```
**Result:** Single deployment/pod in namespace no other workloads present that could be making the `kanasmc` call from within this namespace.

---

## 3. Application Log Review (`api-cims` container)
```
kubectl logs api-cims-56fbf694d6-5gfq9 -n api-cims-v1 -c api-cims | Select-String -Pattern "kanasmc"
```
**Result:** No matches. `kanasmc.alldata.net` never appears anywhere in this pod's application logs.

```
kubectl logs api-cims-56fbf694d6-5gfq9 -n api-cims-v1 -c api-cims | Select-String -Pattern "TokenGenerator","token_type","invalid_client","invalid_grant"
```
**Result:** OAuth token requests to `entapi.alldata.net/cbc/services/oauth2/token` observed token flow active for this service's actual integration.

**Confirmed working calls in logs:**
- `RetrieveCaseService::RetrieveCaseResponse` → real `accountId` values returned (e.g. `1142029582`, `1114407356`) success, no auth failure
- `CreateCaseService::createCase` → calling `entapi.alldata.net/cbc/services/apexrest/CIMS/V1/Complaints/` with Bearer token attached
- All outbound calls from this pod target **`entapi.alldata.net` only** none target `kanasmc.alldata.net`

**Non-critical noise observed:** repeated `WARN` cookie rejected due to domain mismatch (`domain: salesforce.com`, origin: `entapi.alldata.net`) unrelated to the 401, does not block request/response flow.

---

## 4. DNS Resolution (from AKS-accessible host)
```
nslookup kanasmc.alldata.net
nslookup entapi.alldata.net
```
| Host | Resolved IP |
|---|---|
| kanasmc.alldata.net | 10.94.136.16 |
| entapi.alldata.net | 10.37.251.196 |

Both resolve successfully, to **different IPs** confirmed as two separate backend systems, not aliased.

---

## Summary of AKS-side findings
| Check | Result |
|---|---|
| Pod health | Healthy |
| Other workloads in namespace | None single pod |
| App logs reference `entapi.alldata.net` | Present, working correctly |
| DNS resolves `kanasmc.alldata.net` | Yes, distinct IP from `entapi.alldata.net` |


No evidence on the AKS side connects this pod, namespace, or cluster to the failing `kanasmc.alldata.net` call.

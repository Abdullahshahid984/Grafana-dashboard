Yes, aligned with this approach. The current blocker is ensuring OIDC federated credentials for the restore target cluster (POC2) are created on the backup cluster's (DEV) workload Managed Identities to avoid 401 authentication errors. I've created a PR to automate this, and I need to test it to validate it works before moving forward.

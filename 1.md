The application is working fine via the existing ingress hostname, so pods, service, and VirtualService routing are all healthy.

The issue is at the Istio Gateway level — the shared gateway (aks-istio-ingress/istio-ingress-gateway) currently only allows app-catools-v1.ihub-dev-eus2.aks-ingress.breadfinancial.net. The new domain catools-dev.breadfinancial.com is not included in the Gateway hosts, so traffic for that hostname is being dropped at ingress, which explains the timeout.

We’ll need to update the Gateway to include catools-dev.breadfinancial.com

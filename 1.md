Hey team, follow-up on the AKS backup/restore RBAC issue I flagged earlier.

Found the root cause and it's fixed now. The restore cluster (dev-rst) was fighting with the original dev cluster over the same Terraform state, which is why role assignments and federated credentials on the original cluster kept getting overwritten. Now the original cluster runs completely untouched, and dev-rst just gets the one extra credential it actually needs. Also fixed the alert naming so the two clusters don't clash there either.

I've restored this on the RST cluster and it's working fine. Still monitoring dev, dev-rst, and the pipeline closely, all good so far.

I've created a PR with the changes, please take a look, suggestions welcome.

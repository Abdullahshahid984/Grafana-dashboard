Run each line one by one, press Enter after each:

**Line 1:**
```powershell
kubectl get secret kvcustacctappseus2sit-secrets-syncd -n app-catools-v1 -o jsonpath='{.data.CATOOLS_OKTA_SAML_CERTIFICATE}' > cert.txt
```

**Line 2:**
```powershell
$encoded = Get-Content cert.txt
```

**Line 3:**
```powershell
$decoded = [System.Text.Encoding]::UTF8.GetString([System.Convert]::FromBase64String($encoded))
```

**Line 4:**
```powershell
Write-Output $decoded
```

Paste the output of line 4.

Sure! Let's run some tests step by step from the AKS cluster. Run these commands one by one:

---

## Test 1 — Confirm Egress IP
```powershell
kubectl exec -n app-collibradataquality-v1 dq-web-dq-0 -- curl -s https://ifconfig.me
```

---

## Test 2 — Test Port 25 Connectivity
```powershell
kubectl exec -n app-collibradataquality-v1 dq-web-dq-0 -- curl -v --max-time 10 telnet://mail.alldata.net:25
```

---

## Test 3 — Test DNS Resolution from Pod
```powershell
kubectl exec -n app-collibradataquality-v1 dq-web-dq-0 -- curl -s https://dns.google/resolve?name=breadfinancial.com`&`type=MX
```
This uses Google DNS API since `nslookup` is not available in the pod.

---

## Test 4 — Full SMTP Handshake Test
```powershell
kubectl exec -n app-collibradataquality-v1 dq-web-dq-0 -- curl -v --url "smtp://mail.alldata.net:25" --mail-from "dave.dernberger@breadfinancial.com" --mail-rcpt "dave.dernberger@breadfinancial.com"
```

This simulates the exact same SMTP flow the application does and will show if `451` error comes back.

---

## Test 5 — Check if STARTTLS Works
```powershell
kubectl exec -n app-collibradataquality-v1 dq-web-dq-0 -- curl -v --url "smtp://mail.alldata.net:25" --ssl-reqd --mail-from "dave.dernberger@breadfinancial.com" --mail-rcpt "dave.dernberger@breadfinancial.com"
```

---

Run **Test 4 first** — that's the most important one. Share the output and I'll tell you exactly what's happening.

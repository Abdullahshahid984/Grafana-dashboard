Yes, that matches the approach I have in mind. The procedure would be:

1. Configure a new cluster in the POC resource group, running in parallel with the existing DEV cluster in Dev RG. For example, create the new cluster as `poc2`.
2. Update the environment’s `conf.yaml` to include the new cluster, then run the platform upgrade pipeline for that environment to deploy the new cluster.
3. I have created the PR to test and validate the workload onboarding. The pipeline should automatically create the required federated credentials for the restore target cluster (`poc2`) using its OIDC issuer.
4. Once the onboarding is validated, restore the backup from the existing DEV cluster to the new `poc2` cluster using the backup vault.

This will allow us to validate the complete restore workflow, including the automation for creating the required federated credentials for the target cluster.

cp $(Agent.TempDirectory)/terraform.rc ~/.terraformrc

CURRENT=ihub-poc-eus2

cp $(Build.SourcesDirectory)/bfhaksplatform_instances/$CURRENT/conf.yaml \
   $(Build.SourcesDirectory)/bfhaksplatform/terraform/conf.yaml

cd $(Build.SourcesDirectory)/bfhaksplatform/terraform

export TF_CLOUD_HOSTNAME=app.terraform.io
export TF_WORKSPACE=ws-bfhaks-ihub_main-poc-eus2-01

terraform init -reconfigure -no-color

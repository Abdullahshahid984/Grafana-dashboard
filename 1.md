Hi, I have created the PR for automating Workload Identity federated credential creation for additional AKS clusters.

The change allows us to configure additional clusters through a single Terraform variable, dynamically retrieve their OIDC issuer URLs, and create the required federated credentials for all workloads for restore cluster

PR: `feat: Add Workload Identity federated credential support for additional clusters`

PR is ready for review.

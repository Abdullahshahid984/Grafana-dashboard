**Root cause on the AKS side:** the credential `KANA_API_AUTHORIZATION_KEY` is correctly present in Key Vault (confirmed App Service uses this exact same secret successfully) and correctly synced down to the pod's mounted file and Kubernetes Secret object. But it's showing up empty when read as an actual environment variable inside the running `api-cims` pod  while every other secret in that same pod, populates correctly. So this is isolated specifically to this one credential failing to reach the pod's environment, not a Key Vault or access problem.

**What I need:** approval to do a rolling restart of the `api-cims` deployment in `api-cims-v1`. Environment variables from this kind of secret sync are only set once at pod startup, so there's a real chance the pod just has a stale snapshot from before this key was fully synced a restart would force it to pick up the current (correct) value. This is low risk: single-replica rolling restart, no config changes, and this code path isn't currently serving production traffic since it's not the active integration path.

Can you approve this, or point me to who needs to sign off? Happy to walk through the full investigation if useful.

# Check if the token request actually returns 200 vs 4xx
kubectl logs api-cims-56fbf694d6-5gfq9 -n api-cims-v1 -c api-cims | Select-String -Pattern "getAccessToken","access_token","statusCode","response"

# Check config for where username/password should be sourced from
kubectl get configmap -n api-cims-v1
kubectl describe configmap <relevant-configmap> -n api-cims-v1

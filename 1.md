Hey team, quick update on the AKS backup/restore situation and what actually caused the application team's issues.

I set up a restore/validation cluster (dev-rst) using Azure Backup to test that our backups actually restore correctly, without risking the original dev cluster. While setting it up, it caused an RBAC issue on the original dev cluster, role assignments and federated credentials were getting overwritten because the restore cluster was fighting with the original cluster over the same Terraform state. I found the root cause and fixed it the same day. Now the original cluster is fully untouched, and the restore cluster just gets the one extra credential it needs. Also fixed alert naming so the two clusters don't clash.

Note: while fixing this, I ran the pipeline a few times from my feature branch, so ignore any failed runs in the history. Build 274 in workload onboarding is the successful, deployed fix.

I've restored this on the RST cluster and it's working fine. Still monitoring dev, dev-rst, and the pipeline closely, all good so far.

Also, Sahil flagged Wiz Broker disconnecting on dev-rst, checked it out, pod's running and Wiz is connected fine, just a transient blip, no fix needed.

To be clear: only the RBAC issue was actually caused by the backup setup, and it's fixed. Nothing else traces back to AKS backup.

Also, the pipeline is currently failing since my fix is still in a feature branch. I had to merge the RST cluster creation to main separately since it needed an Istio version bump (1.28 to 1.29) to actually get created. So main now has the RST cluster but not the alert fix yet, which is why the pipeline is trying to recreate alerts that already exist, same conflict as before. This will resolve once my PR is merged.

I've created a PR with the changes, please take a look, suggestions welcome. What changed:

- Restore cluster no longer runs as its own separate cluster in the pipeline, it rides along with the original cluster's run
- Removed the logic that was renaming the original cluster's RBAC/credentials
- Restore cluster still gets its own federated credential via the existing "additional cluster" mechanism
- Fixed alert naming to check per-cluster instead of one flag for all
- Simplified the pipeline script back to normal, no special workspace routing for restore cluster

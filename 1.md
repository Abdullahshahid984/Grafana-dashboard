```
I need to add a new API operation, POST /auth/batch, to the Fiserv Rewards PLP API in this repo. Make exactly these two edits, and change nothing else. Do not reformat either file.

1. In APIM/apis/fiservrewardsplpapi/FiservRewardsPLP.yaml:
Under `paths:`, after the last existing path block (`/summary`) and before `components:`, add this block, using 2-space indentation like the other paths:

  /auth/batch:
    post:
      summary: authBatch
      description: authBatch
      operationId: authBatch
      responses:
        '200':
          description: 'null'

2. In APIM/apis/fiservrewardsplpapi/arm/FiservRewardsPLP.api.template.json:
The OpenAPI spec is embedded as one long string in the "value" property of the API resource, with literal \r\n escape sequences. Find the literal text `\r\ncomponents:` inside that string. Insert the following text immediately before it, right after the `description: 'null'` that ends the /summary block. Keep the \r\n sequences as literal backslash characters, not real line breaks:

\r\n  /auth/batch:\r\n    post:\r\n      summary: authBatch\r\n      description: authBatch\r\n      operationId: authBatch\r\n      responses:\r\n        '200':\r\n          description: 'null'

Requirements:
- The JSON file must remain valid JSON after the edit.
- Do not use Format Document or change any other line, whitespace, or line endings.
- After editing, show me the git diff so I can verify that only these additions were made.
```

All pods are stuck in Init:0/2 status (PodInitializing) they're failing at the volume mount stage before any containers can start. Root cause: The Secrets Store CSI Driver can't mount the Key Vault volume because it's unable to retrieve the PRIMARY-DB2-PASSWORD secret from Azure Key Vault (kvadscusperfihub02). All pod events show repeated FailedMount warnings with RPC errors on the GET request.

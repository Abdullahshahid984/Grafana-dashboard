Good! You found the correct NSG name: `aks-agentpool-1285725-nsg`

Now run this to see all outbound rules:

```powershell
$nodeRG = "rg-bfhaks-ihub-eus2-dev-01-nodes"
$nsgName = "aks-agentpool-1285725-nsg"

# Check all outbound rules
az network nsg rule list -g $nodeRG --nsg-name $nsgName --query "[?direction=='Outbound']" -o table
```

The command at the end of your screenshot is running but cut off. Let me give you a cleaner version:

```powershell
az network nsg rule list -g rg-bfhaks-ihub-eus2-dev-01-nodes --nsg-name aks-agentpool-1285725-nsg -o table
```

Run that and paste the full output. We're looking for:
1. Is there a rule allowing port 53 (DNS)?
2. Is there a rule allowing `*` (all ports)?
3. What's the destination for outbound rules?

If port 53 isn't in the outbound rules, that's your problem - DNS queries are blocked.

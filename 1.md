Hi, I’m trying to run the POC pipeline, but it’s failing with the error: `bfhaksplatform/cicd/setup-terraform-main.sh: No such file or directory` (exit code 127). It looks like the task group might still be pointing to the old script path.

Could you please update the task group according to the recent changes so I can proceed with running the pipeline?

Thanks!

# Check the application.properties or application.yml in the image
kubectl get deployment api-transactions -n api-transactions-v1 -o yaml | Select-String -Context 0,20 "datasource"

# Or check if there's a properties file mounted
kubectl exec -it <running-pod> -n api-transactions-v1 -- cat /app/application.properties

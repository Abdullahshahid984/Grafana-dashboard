Root cause confirmed and fixed: KANA_API_AUTHORIZATION_KEY was empty in the AKS pod's environment due to a stale snapshot from pod startup — the value was correct in Key Vault, the CSI-mounted file, and the K8s Secret object the whole time, but hadn't been picked up into the running pod's environment variables. A rolling restart of api-cims resolved it. Verified via APIM: Kana Case Validation (case 20258796) now returns 200 OK on both CIMS API (App Service) and CIMS API AKS, where it previously returned 401 on the AKS path.

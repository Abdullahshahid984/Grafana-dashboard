**CIMS Process API 401 Investigation PRD-CUS**

We had a 401 Unauthorized error on a Kana case validation call (kanasmc.alldata.net) tied to the CIMS Process API. The original failure was traced to AKS, and the route was switched to Azure App Service which is the current, healthy production path. No active outage.

Separately, while testing AKS's own (currently unused) implementation of the same integration, I reproduced the same 401 directly, with a full stack trace confirming the exact failure point.

The credential AKS needs (`KANA_API_AUTHORIZATION_KEY`) is correctly stored in Key Vault confirmed, since App Service uses this exact same secret successfully. It also correctly syncs down into the AKS pod's file system and into the underlying Kubernetes Secret object. But when the application reads it as an environment variable, it comes back empty.

I checked every other secret in the same pod, and a secret in a completely separate application using the same Key Vault all populate correctly. This is isolated specifically to this one credential, in this one pod's environment.

Kubernetes env vars set this way are populated once, at pod startup they don't auto-refresh if the underlying secret changes afterward. This pod hasn't restarted since 7/22, so it likely captured a stale/incomplete snapshot at startup. The current secret data is confirmed correct; a rolling restart would force a re-read.

I have ruled out every other explanation testable from my access level: Key Vault content, the sync mechanism, permissions, and whether this is a broader pattern (it isn't).

**Ask:**
The developer suggested either:
1. Getting sign-off now for a restart, or
2. Validating this as part of next week's scheduled deployment (which restarts the pod anyway)

Risk profile Low either way single replica, rolling restart, no config changes. This code path isn't currently handling production traffic since App Service is the active path.

Would love your thoughts on this.

I created the `dev-rst` cluster in the Dev RG and installed the Backup extension on the RST cluster.

Once the cluster was created, I reused the federated credential code I had implemented for `poc-02` and updated it for `dev-rst`.

When I ran the Workload Onboarding pipeline, I noticed that the Prometheus app alert rules were being created in the Dev RG for the RST cluster. I knew this would fail because the same alert rules already exist for the original Dev cluster, so I cancelled the run.

I then added Terraform code to skip the Prometheus app alerts for the RST cluster. After rerunning the pipeline, the alert-related issue was resolved.

The next issue was related to the Terraform Cloud workspace. The pipeline was looking for a workspace associated with the RST Dev cluster, which does not currently exist. Creating a new workspace would also not work because it would not have the required state file.

To address this, I modified the Workload Onboarding PowerShell script to handle the RST cluster using an `if/else` condition:

* If the cluster is `dev-rst`, use the `dev-rst` workspace.
* Otherwise, continue using the existing workspace for the respective environment.

After this change, the workspace issue was resolved.

The next issue was related to missing Istio gateways. I added the required Istio gateways for the RST cluster in `conf.yaml`, after which the Workload Onboarding pipeline completed successfully.

During validation, I noticed an important RBAC issue: the pipeline was revoking the namespace RBAC permissions from the original Dev cluster and assigning them to `dev-rst`. Since this could impact the existing Dev cluster, I proceeded with the remaining validation quickly.

I restored the workload on the RST cluster and confirmed that it was working successfully. I have attached screenshots below showing the successful workload restoration and validation.

After completing the validation, I deleted the `dev-rst` cluster.

I have also commented out the temporary Workload Onboarding changes while I work on resolving the RBAC issue.

My current approach is to update the TFC code so that the RST cluster receives its own RBAC configuration without modifying or revoking the existing namespace permissions from the original cluster.

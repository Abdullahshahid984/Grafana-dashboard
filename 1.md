**Option 1 - Search for RBAC roles:**
```bash
az role assignment list \
  --scope /subscriptions/{subscription-id}/resourceGroups/rg-bfhaks-ihub-eus2-dev-01/providers/Microsoft.ContainerService/managedClusters/aks-bfhaks-ihub-eus2-dev-01 \
  --output table | grep -i "RBAC"
```

**Option 2 - Search for app team principal:**
```bash
az role assignment list \
  --scope /subscriptions/{subscription-id}/resourceGroups/rg-bfhaks-ihub-eus2-dev-01/providers/Microsoft.ContainerService/managedClusters/aks-bfhaks-ihub-eus2-dev-01 \
  --output table | grep -i "App-Team"
```

**Option 3 - Search for pipeline credential:**
```bash
az role assignment list \
  --scope /subscriptions/{subscription-id}/resourceGroups/rg-bfhaks-ihub-eus2-dev-01/providers/Microsoft.ContainerService/managedClusters/aks-bfhaks-ihub-eus2-dev-01 \
  --output table | grep -i "harness"
```

**Option 4 - Show ALL roles (no grep):**
```bash
az role assignment list \
  --scope /subscriptions/{subscription-id}/resourceGroups/rg-bfhaks-ihub-eus2-dev-01/providers/Microsoft.ContainerService/managedClusters/aks-bfhaks-ihub-eus2-dev-01 \
  --output table
```

## Summary

Removed all Terraform code added for RST cluster restore validation support federated credentials and Prometheus alert exclusion logic  as the RST cluster is being removed from the platform configuration entirely.

## Premise

### `phases/01-per-instance/variables.tf`
- Removed `restore_clusters` variable

### `phases/01-per-instance/main.tf`
- Removed `data "azurerm_kubernetes_cluster" "restore_clusters"` data source
- Removed `restore_cluster_oidc_issuer_urls` from `module "app_alerts"`
- Removed `excluded_cluster_names` from `module "app_alerts"`

### `phases/01-per-instance/locals.tf`
- Removed `excluded_cluster_names` local

### `modules/kubernetes-resources/variables.tf`
- Removed `restore_cluster_oidc_issuer_urls` variable

### `modules/kubernetes-resources/main.tf`
- Removed `azurerm_federated_identity_credential.workload_identity_restore` resource

### `modules/app_alerts/variables.tf`
- Removed `excluded_cluster_names` variable

### `modules/app_alerts/main.tf`
- Removed `if !contains(var.excluded_cluster_names, cluster.name)` filter


**Justification:**

AKS pod is reading `KANA_API_AUTHORIZATION_KEY` as empty due to a stale env var snapshot from startup. A rolling restart is needed to force re-read of the current, correct Key Vault secret and resolve the 401 error.

**Implementation plan:**

Perform a rolling restart. No config or code changes involved.

**Risk and impact analysis:**

What is the impact if any issues arise?

Minimal single replica, standard rolling restart of an unused code path (App Service is the active production integration, not AKS).

What could the impacted areas/users see if issues were to arise? 

No user impact expected; this path carries no production traffic.

**Known Impact/Outage:** Unchecked (none)

**Backout plan:**

No backout needed restart only re-reads existing secret values. If the pod fails to come back up, standard rollback via previous ReplicaSet/deployment revision applies.

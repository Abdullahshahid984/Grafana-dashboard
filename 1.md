```
Hi Team,I'm troubleshooting a persistent database connection issue following a namespace recreation in our AKS cluster. Our application pods inside the api-statements-v1 namespace are failing to initialize and are currently stuck in a CrashLoopBackOff with the following error:Prelogin error: host acsi6x014.database.windows.net port 1433 Error reading prelogin response: Connection resetOur pods are utilizing Azure Workload Identity and traffic is leaving the cluster successfully, but the connection is being dropped forcefully at the pre-login handshake phase by the database firewall.Upon checking the Private Access configurations for acsi6x014.database.windows.net in the Azure Portal, I noticed that private endpoints are exclusively provisioned for SIT workloads (...-sit-01...). We do not see an active Private Endpoint mapping for our DEV cluster subnet layer.
Could you please confirm, is this specific SQL server (acsi6x014) restricted exclusively to SIT workloads, meaning we should be pointing our deployment to a separate DEV SQL database instance?

```

```
bash -c 'timeout 5 bash -c "cat < /dev/null > /dev/tcp/acsi6x014.database.windows.net/1433"' && echo "Connected" || echo "Connection refused/timeout"
```
```
nslookup acsi6x014.database.windows.net
```

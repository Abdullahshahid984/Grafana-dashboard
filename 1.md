Hey yes, the Helm config for ProfileAPI supports this. It's already mounting secrets from Key Vault as volumes via the CSI Secret Provider. To mount a certificate, we just need to add it to the secretProviders block in the values with the cert name from Key Vault and the desired mountPath.

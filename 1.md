kubectl get ns -o jsonpath='{.items[*].metadata.name}' | ForEach-Object { $_ -split ' ' } | Where-Object { $_ -notmatch '^(kube-system|kube-public|kube-node-lease|default|gatekeeper-system|calico-system)$' -and $_ -ne "" } | ForEach-Object { kubectl delete ns $_ --force --grace-period=0 }

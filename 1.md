Hi Team,

I have completed a cluster-level diagnostic check, and here are the confirmed findings:

RBAC Verification in UAT:
- Ran kubectl auth can-i to test the service principal's actual permissions
- Result: EXPLICIT YES for listing secrets across all namespaces
- Conclusion: The cluster has proper RBAC configured this is NOT a permission issue

Since RBAC is confirmed working, the deployment failure must be happening elsewhere in the pipeline.

Could you please check the following in Harness to help identify what's blocking the deployment:

1. Check the deployment cache for this service pipeline - is it serving a stale/outdated build artifact?
2. Verify if a recent change was made to the pipeline template or configuration
3. Check if new commits from Git are actually being pulled, or if Harness is stuck on an older version
4. If there were recent changes to ConfigMaps or SecretProviderClass configuration in Harness, please review those as well

Since I don't have Harness access, I can't dig deeper on the pipeline side. But the cluster itself is healthy and permissions are correct.

please investigate the Harness cache/configuration and let me know if clearing or updating anything resolves the issue?

Thanks!

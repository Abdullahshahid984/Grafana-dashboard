After the rolling restart, verify the pod has picked up the correct KANA_API_AUTHORIZATION_KEY value by checking the pod environment variable, then re-run the Kana case validation call that previously returned 401 and confirm it now succeeds (200 response, no auth error) in the stack trace/logs.

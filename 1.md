I have investigated the issue and found no changes implemented on our side in the Analytics prdeus2 cluster yesterday.
Since no changes were made on our end, this may have been triggered by a Kubernetes self-healing mechanism.
I also investigated the file path backing the upload directory:

• emptyDir  ephemeral, pod-local storage

• Any PVC in namespace none found

• Conclusion: zero persistent storage exists in app-collibraedge-v1

The lineage data is lost on pod restart because the upload path is backed by ephemeral storage with no persistence layer behind it.
Recommended Path Forward

Short term: Continue manually reloading the file after pod restarts as we currently do.

Long term:

Mount a PVC:

Modify the deployment to mount a new PersistentVolumeClaim. The file would survive pod restarts without any manual intervention.

Happy to work through this option with the team when ready.

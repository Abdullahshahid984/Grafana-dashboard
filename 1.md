
```powershell
# Check if a configmap reader role exists
kubectl get role -n api-transactions-v1 | Select-String -Pattern "configmap"

# List all roles in the namespace
kubectl get role -n api-transactions-v1

# Check all rolebindings for the service account
kubectl get rolebinding -n api-transactions-v1 | Select-String -Pattern "sa-api-transactions"

# List all rolebindings
kubectl get rolebinding -n api-transactions-v1
```


```powershell
kubectl get clusterrole | Select-String -Pattern "configmap"

kubectl get clusterrolebinding | Select-String -Pattern "api-transactions"
```

If those don't exist, then the RBAC was never set up. But if they do exist and the `auth can-i` test still says "no", then something removed the permissions.

Run those commands and share what you find. This will tell us if this is a missing configuration or if something broke it.

**Title:** feat(istio-gateway): add custom domain support for workloads

**Problem**

The shared Istio Gateway only accepted auto-generated hostnames. There was no way to add a custom domain like `catools-dev.breadfinancial.com` without a manual kubectl patch that gets overwritten on the next Terraform apply.

**Changes**

`schema/workload_onboarding-conf.yaml` - Added `extra_gateway_hosts` property to the `istio` block.

`schema/instance-conf.yaml` - Added `extra_sans` property to the `istio_ingress_gateway` items.

`workload-onboarding/use_cases/ihub/workloads/catools-v1.yaml` - Declared `catools-dev.breadfinancial.com` as a custom host for `ihub-dev-eus2`.

`workload-onboarding/phases/02-per-instance-per-cluster/locals.tf` - Updated `istio_gateway_hosts` to merge auto-generated and custom hosts using `concat()`. Uses `try()` for backward compatibility with workloads that do not declare `extra_gateway_hosts`.

`bfhaksplatform_instances/ihub-dev-eus2/conf.yaml` - Added `extra_sans` with `catools-dev.breadfinancial.com` to the `istio_ingress_gateway` block.

`bfhaksplatform/istio-ingress-gateway/cicd/install.sh` - Updated cert-manager certificate block to append `extra_sans` to `dnsNames`.


2. Trigger Terraform Phase 2 pipeline for `ihub-dev-eus2` to apply the updated Gateway.
3. App team to add `catools-dev.breadfinancial.com` to their VirtualService.
4. DNS team to point `catools-dev.breadfinancial.com` to the Istio ingress LB IP.

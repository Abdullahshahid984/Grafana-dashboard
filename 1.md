cp $(Agent.TempDirectory)/terraform.rc ~/.terraformrc

cp bfhaksplatform_instances/current/conf.yaml bfhaksplatform/terraform/conf.yaml


cd bfhaksplatform/terraform

terraform init -no-color

terraform apply -no-color

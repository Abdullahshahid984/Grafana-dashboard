**Title:**
ProfileAPI SIT: Add CATOOLS-OKTA-SAML-CERTIFICATE to SecretProviders Volume Mount


 **Summary**
Helping api-profile-v1 to add the `CATOOLS-OKTA-SAML-CERTIFICATE` certificate from Key Vault `kvcustacctappseus2sit` to the ProfileAPI `values.yaml` secretProviders block so it is mounted as a volume inside the pod and accessible via file path by the application.

Hi, Just flagging that both the platform upgrade and workload onboarding pipelines are currently failing at the configure monitoring stage due to a timeout error when reaching the DEV AKS API server.

Error:
Dial tcp 10.41.244.36:443: i/o timeout — the pipeline agent is unable to download the OpenAPI schema from https://aks-bfhaks-ihub-eus2-dev-01.private.eastus2.azmk8s.io:443

This stage was passing successfully prior to the VNet integration, so I believe the integration may have affected the pipeline agent's connectivity to the private AKS API server. Could you please take a look?

Thanks

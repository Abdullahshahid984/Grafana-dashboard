Hi,

Could you please review and approve the following PR when you get a chance?

PR: <link>

As part of the AKS backup and restore validation, we are restoring DEV workloads to the POC-02 cluster. The restored workloads use container images from the DEV ACR (acrbfhaksihubeus2dev02.azurecr.io) but the POC Azure Policy only allows images from the POC ACR, causing pods to fail with an admission webhook denial error.

This PR adds the DEV ACR to the POC allowed registries list to unblock the restore validation. It is a temporary change and will be reverted once validation is complete.

Thanks,
Abdullah

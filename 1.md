

```powershell
# See what's inside the secret
kubectl get secret kvadseus2nonprdsi1hub01-secrets-syncd -n api-transactions-v1 -o yaml

# Or just the keys:
kubectl get secret kvadseus2nonprdsi1hub01-secrets-syncd -n api-transactions-v1 -o jsonpath='{.data}' | ConvertFrom-Json
```

```powershell
kubectl get configmap api-transactions-config -n api-transactions-v1 -o yaml
```

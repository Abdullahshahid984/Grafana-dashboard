Hi , raised a PR for a fix in install.sh  the certificate generation step was failing because yq outputs multiline JSON by default, which breaks env() parsing with an EOF error. Fix splits it into two yq calls and uses -I=0 to force compact single-line JSON before shell interpolation. Could you review and approve when you get a chance?

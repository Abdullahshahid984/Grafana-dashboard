Agreed. Renamed it and updated the references in outputs.tf and the app_alerts module call.

Fair point. The rule group name is scoped to the resource group, so any multi-cluster instance would collide. Here, *-rst-* clusters are filtered out in phase 1 before reaching this module, so the rename branch was unreachable and I reverted it to avoid dead code. Making names cluster-aware would replace existing rule groups, so I'd handle that separately if a second non-restore cluster is added to an instance.

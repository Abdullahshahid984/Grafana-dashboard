Hi, I have created two related PRs for the DEV-RST restore cluster implementation. When you get a moment, could you take a look?

**PR 1: Add DEV-RST Cluster Configuration**
- Adds DEV-RST cluster definition to DEV environment conf.yaml
- Provisions new AKS cluster in same resource group as DEV (rg-bfhaks-ihub-eus2-dev-01)
- Enables disaster recovery testing in isolated cluster within same RG

**PR 2: Add Federated Credentials for DEV-RST Restore Target**
- Updates workload onboarding to automatically create federated credentials for DEV-RST's OIDC issuer

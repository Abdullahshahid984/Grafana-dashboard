Hi Jeffrey, you were correct the variable was being set but not consumed. I have pushed a fix. The + (env(EXTRA_SANS_JSON) | from_json) is where the variable is consumed it appends the extra SANs to the dnsNames list. Thank you for catching that.

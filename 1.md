Adds the POST /auth/batch operation to the Fiserv Rewards PLP API on the partner APIM, as requested in the ticket. This mirrors how the existing operations (for example the redemption batch) are defined.

kubectl delete ns $(kubectl get ns -o jsonpath='{.items[*].metadata.name}' | tr ' ' '\n' | grep -vE 'kube-system|kube-public|kube-node-lease|default|gatekeeper-system|calico-system') --force --grace-period=0

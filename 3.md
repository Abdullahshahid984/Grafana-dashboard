backup_instance is defined in the instance configuration schema. It appears in the schema file around line 305, where the backup_instance array and its properties (aks_cluster, backup_policy, etc.) are defined. 

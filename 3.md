Was the instance-boiler-template branch supposed to overwrite the Terraform structure?
Because the pipeline runs terraform apply from the previous path, and now it looks like there are no .tf files in that directory.

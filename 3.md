According to the Terraform provider documentation, included_namespaces is an optional parameter used to restrict backups to specific namespaces. we are not defining any namespace filters both included and excluded lists are empty, no filtering is applied and the backup includes all namespaces. The Terraform plan also confirms that no filters are configured in backup_datasource_parameters.


https://registry.terraform.io/providers/hashicorp/azurerm/latest/docs/resources/data_protection_backup_instance_kubernetes_cluster#included_namespaces-1

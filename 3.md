Agreed. I have made min_tls_version a configurable variable with a default of TLS1_2. This keeps us aligned with current security requirements while allowing us to roll forward to TLS 1.3 in a controlled, per-environment manner when it becomes available.

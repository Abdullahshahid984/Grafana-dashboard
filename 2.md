Azure Backup restores Prometheus alert rule groups to Azure as part of the namespace restore. However, these same rule groups are also managed by our workload onboarding Terraform pipeline (Phase 01). When the pipeline runs, it tries to create the same rule groups that Azure Backup already restored causing a conflict:

```Resource already exists in Azure but not in Terraform state → Pipeline fails```

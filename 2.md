We added the RST cluster to conf.yaml which caused the pipeline to create Prometheus alert rule groups for it. But Azure Backup already restored those same rule groups into the same resource group with the same name so when Terraform tried to create them, Azure rejected it with "resource already exists" error, breaking the DEV pipeline. Currently working on resolve this issue 

#schema file need to be update at line 943
        
        exclude_prometheus_alerts:
           type: boolean
           description: >
            (optional) exclude this cluster from Prometheus alert rule creation.
            Set to true for restore/validation clusters to avoid conflicts with
            Azure Backup restored resources. Defaults to false.


# conf.yaml file of dev cluster
exclude_prometheus_alerts: true

aks_backup_rbac:
    type: object
    description: Enable or disable automatic RBAC configuration for AKS Backup
    properties:
      enabled:
        type: boolean
    required:
      - enabled
    additionalProperties: false
